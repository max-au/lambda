{erl_opts, [debug_info]}.
{deps, [lambda]}.

{relx, [
    {release, {vdemo, "0.1.0"}, [vdemo, lambda, sasl]},
    {debug_info, keep},
    {sys_config, "./config/sys.config"},
    {vm_args, "./config/vm.args"}
]}.
